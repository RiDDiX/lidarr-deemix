# deezer-py handles the ARL login and the whole Deezer API.
# The old deemix package was only used by the removed /download endpoint.
# mitmproxy comes from apk in the runtime image (see Dockerfile), not from pip.
deezer_py==1.3.7
Flask==3.1.*
waitress==3.0.2
